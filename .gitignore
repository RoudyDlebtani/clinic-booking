# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*

# testing
coverage

# build output
dist
build

# misc
.DS_Store
*.pem

# vercel
.vercel

# NOTE: api/index.js is the esbuild-bundled serverless function. It is committed
# (not ignored) so git-based Vercel deploys include the function. Rebuild it with
# `npm run build:api` after changing server code.

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.*
!.env.example
!.env.local.example

# typescript
*.tsbuildinfo
